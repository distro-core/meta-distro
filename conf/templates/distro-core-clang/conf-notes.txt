You can now run 'bitbake <target>'

Common targets are:
    distro-image
    distro-sdk-image

Common bitbake arguments:
    bitbake -e <target>           dump environment
    bitbake -DD <target>          enable DEBUG: messages
    bitbake -c clean <target>     clean
    bitbake -g <target>           generate recipe dependency

---
