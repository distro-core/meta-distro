# conf/distro/include/distro-version.inc

DISTRO = "distro-core"
DISTRO_VERSION = "0.0.0"

DISTRO_NAME = "DISTRO Core (${DISTRO} ${TCLIBC})"
MAINTAINER = "RPM Signing <release@internal>"

TARGET_VENDOR = "-distro"

# ---
